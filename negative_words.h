/*#include <unordered_map>
#include<cstdio>

using namespace std;
class NegativeWords {
    private:
    unordered_map<string, long long> negativeWordsMap;

    public:
    NegativeWords();
    long long getFrequency(string phrase);

};
